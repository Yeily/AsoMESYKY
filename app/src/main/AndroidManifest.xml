<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="asomesyky.webhostapp.com">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Actividades.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Actividades.MenuActivity" />
        <activity android:name=".Actividades.InversionActivity" />
        <activity android:name=".Actividades.AdminActivity" />
        <activity android:name=".Actividades.RegistroActivity" />
        <activity android:name=".Actividades.AporteActivity" />
        <activity android:name=".Actividades.PasswordActivity" />
        <activity android:name=".Actividades.LiquidaInvActivity"></activity>
    </application>

</manifest>